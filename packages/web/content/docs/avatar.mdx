---
title: 'Avatar'
description: 'Interactive button component with various styles and states.'
category: components
---

# Avatar

A button component that follows best practices for accessibility and provides multiple variants for different use cases.

## Usage



## Features

- Multiple variants and sizes
- Accessible by default with ARIA attributes
- Support for icons and loading states
- Keyboard navigation support

## Examples

### Variants

Buttons come in different variants to match your design needs.

<Tabs defaultValue='default'>
    <TabsList>
        <TabsTrigger value='default'>Default</TabsTrigger>
        <TabsTrigger value='secondary'>Secondary</TabsTrigger>
        <TabsTrigger value='gradient'>Gradient</TabsTrigger>
        <TabsTrigger value='ghost'>Ghost</TabsTrigger>
        <TabsTrigger value='link'>Link</TabsTrigger>
    </TabsList>
    <TabsContent value='default'>
        <ComponentPreview
        name='button'
        />
    </TabsContent>
    <TabsContent value='secondary'>
        <ComponentPreview
        name='secondary_button'
        />
    </TabsContent>
    <TabsContent value='gradient'>
        <ComponentPreview
        name='gradient_button'
        />
    </TabsContent>
    <TabsContent value='ghost'>
        <ComponentPreview
        name='ghost_button'
        />
    </TabsContent>
    <TabsContent value='link'>
        <ComponentPreview
        name='link_button'
        />
    </TabsContent>
</Tabs>

### Sizes

Available in different sizes to fit your layout.

```tsx
<Avatar size="sm">Small</Avatar>
<Avatar size="md">Medium</Avatar>
<Avatar size="lg">Large</Avatar>
```
### Shapes

Available in different shapes to fit your layout.

```tsx
<Avatar shape="circle" src='https://your-img-url.com' alt='image'/>
<Avatar shape="square" src='https://your-img-url.com' alt='image'/>
```


### With Icons

Icon buttons are a special size variant designed to fit icons perfectly. Fatcn ships with `hugeicons-react` which is our
recommended icon pack for this design system.

<ComponentPreview name="icon_button" />

## Props

| Prop        | Type                                                         | Default     | Description                                                |
| ----------- | ------------------------------------------------------------ | ----------- | ---------------------------------------------------------- |
| `variant`   | `'primary' \| 'secondary' \| 'outline' \| 'ghost' \| 'link'` | `'primary'` | The visual style of the button                             |
| `size`      | `'sm' \| 'md' \| 'lg'`                                       | `'md'`      | The size of the button                                     |
| `disabled`  | `boolean`                                                    | `false`     | Whether the button is disabled                             |
| `loading`   | `boolean`                                                    | `false`     | Shows a loading spinner and disables the button            |
| `asChild`   | `boolean`                                                    | `false`     | Merge props onto child element instead of rendering button |
| `className` | `string`                                                     | -           | Additional CSS classes                                     |

## Design Guidelines

- Use primary buttons for main actions
- Secondary buttons for alternative actions
- Outline buttons for less prominent actions
- Ghost buttons for subtle actions within a busy interface
- Link buttons for navigation-like actions

## Accessibility

The Button component follows WAI-ARIA guidelines:

- Uses native `<button>` element by default
- Properly handles disabled states
- Includes appropriate ARIA attributes
- Supports keyboard navigation
- Maintains sufficient color contrast
