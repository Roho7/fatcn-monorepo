{
  "name": "fatcn-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "clean": "turbo run clean",
    "lint": "turbo run lint"
  },
  "packageManager": "yarn@1.22.22",
  "devDependencies": {
    "turbo": "^2.3.3",
    "@changesets/cli": "^2.27.11",
    "typescript": "^5.7.2"
  }
}